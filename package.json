{
  "private": true,
  "scripts": {
    "build": "slidev build",
    "buildgithub": "slidev build --base /correctexamslides && rm -rf docs && mv dist docs && git add docs/** && git commit . -m 'update' && git push",
    "dev": "slidev --open",
    "export": "slidev export"
  },
  "dependencies": {
    "@slidev/cli": "^0.28.10",
    "@slidev/theme-default": "*",
    "@slidev/theme-seriph": "*"
  },
  "name": "correctexam",
  "devDependencies": {
    "playwright-chromium": "^1.20.1"
  }
}
